import reactCSS from 'reactcss'

export default reactCSS({
  default: {
    originImg: {
      width: '500px',
    },
    list: {
      display: 'flex',
      flexDirection: 'row',
      flexWrap: 'nowrap',
      overflowX: 'auto'
    }
  }
})